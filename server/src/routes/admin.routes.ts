import { Router } from "express";

const router = Router();

// Admin routes — skipped (Phase 5), not interview-critical
// Can be expanded in V2 with LLM call logs and cost tracking

export default router;
